# application.properties
# : 애플리케이션 실행 시 설정 파일
# - 언어 Key = Value 형태의 단순 텍스트
# >> 서버 설정,
# >> DATABASE 연결 정보 설정
# >> JPA/Hibernate 설정
## >> 로깅/이메일/파일 업로드/보안 등 런타임 환경설정

spring.application.name=k5_iot_springboot

server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/k5_iot_springboot
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.show-sql=true
# 기본값은 false
# : 콘솔에 실행되는 SQL 로그 출력(SQL 확인용으로 유용 - 개발 단계에서만 사용해야함)

spring.jpa.hibernate.ddl-auto=update
# : JPA 가 entity를 보고 DB 스키마를 어떻게 관리할지 결정해줌
# - update 옵션은 변경 사항만 저장하게 됨(개발 초기 단계에서만 유용함 -> 운영 시엔 제거를 권장)
#           >> 가능한 범위 내에서 ADD/ALTER 등을 시도해 스키마를 엔티티에 맞춰 수정도 가능함


#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 지금은 위에꺼 따로 안해도 됨

